spring:
  application:
    name: llm-adapter-service

server:
  port: ${SERVER_PORT:8081}

ollama:
  base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
  model: ${OLLAMA_MODEL:llama3}
  temperature: ${OLLAMA_TEMPERATURE:0.2}

mcp:
  base-url: ${MCP_BASE_URL:http://localhost:8083}
